version: '3.9'

services:
  cypress:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/e2e
    networks:
      - default
    entrypoint: ["cypress", "run"]

networks:
  default:
    driver: bridge
